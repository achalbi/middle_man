<%= form_for(@user_workflow_status) do |f| %>
  <% if @user_workflow_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_workflow_status.errors.count, "error") %> prohibited this user_workflow_status from being saved:</h2>

      <ul>
      <% @user_workflow_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :workflow_id %><br>
    <%= f.text_field :workflow_id %>
  </div>
  <div class="field">
    <%= f.label :workflow_status %><br>
    <%= f.text_field :workflow_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
