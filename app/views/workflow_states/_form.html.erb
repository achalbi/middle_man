<%= form_for([@workflow, @workflow_state]) do |f| %>
  <% if @workflow_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workflow_state.errors.count, "error") %> prohibited this workflow_state from being saved:</h2>

      <ul>
      <% @workflow_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :workflow_id, value: @workflow.id  %>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
